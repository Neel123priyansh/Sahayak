# Component Reuse Analysis

- `surface-card` + `ios-shadow`: common card container used for onboarding steps, entry actions, study sections, and video generation.
- Class Cards: consistent grid of cards (Class 1–6) used in onboarding and study mode selection.
- Header Buttons: theme toggle and quick Teach/Study actions reused across pages.
- Input styles: rounded inputs/selects with unified border and background for visual consistency.
- Progress/Loading: pulse animation reused for AI generation and can be reused for other fetch flows.

